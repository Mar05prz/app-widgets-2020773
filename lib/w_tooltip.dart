import 'package:flutter/material.dart';

class EstadoTooltip extends StatefulWidget {
  @override
  State<EstadoTooltip> createState() => _EstadoTooltip();
}

class _EstadoTooltip extends State<EstadoTooltip> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Widget EstadoTooltip"),),
      body: Center(
      ),
    );
  }
}